# Context policy
Every required context block must remain eligible for evaluation. Retrieval may change processing order but must never filter a required block from the final coverage contract.

# Evaluation
A final score is invalid until coverage reaches 100%. Failed blocks remain pending and can be resumed from the same job checkpoint.
