# RAGFlow PR #20207 — independent review probe

**Date:** 2026-09-26  
**PR:** https://github.com/infiniflow/ragflow/pull/20207  
**Pinned PR head:** `7ce7c7e7375062eae15f33dd471788b9189a48fd`  
**Reality status:** confirmed review gap; upstream comment write blocked

## What changed in reality

PR #20207 now occupies the direct-fix path for RAGFlow #20140, so ContextMesh should **not** open a competing PR.

Instead, the useful action is independent review.

The PR correctly addresses the sole-active-instance fallback that our earlier 32K-vs-128K regression exposed. But its head still contains a second service-layer path where the same tenant field keeps two meanings.

On the pinned PR head, `modelInfoWithTenantExtra()` still applies:

```go
model.MaxOutput = extra.MaxTokens
model.MaxTokens = extra.MaxTokens
```

and `ModelSolver.ResolveModelConfig()` still applies `maxTokensFromTenantModelExtra(...)` before returning `ModelTarget.MaxTokens`.

If `tenant_model.extra.max_tokens` is a **context-window override**, then assigning it to generation `MaxOutput` is still a semantic leak.

The executable PR-head check emitted:

```text
CONFIRMED_20207_REVIEW_GAP tenant_extra.max_tokens=context_override remaining_service_effect=ModelInfo.MaxOutput+ModelTarget.MaxTokens
```

## Why this is useful upstream

This is not a competing implementation. It is a review invariant:

> With catalog `MaxOutput = X` and tenant `extra.max_tokens = Y`, applying the tenant context override must not mutate generation `MaxOutput` from X to Y.

That regression can be added without adopting ContextMesh or any ContextMesh runtime.

## D24 stays separate

The separate D24 probe proves that current `chat_pipeline` uses `target.MaxTokens` / catalog `MaxOutput` as the message-fit budget even while `ModelTarget.ContextLength` exists.

That should remain a separate issue/fix so #20207 does not become a catch-all patch.

See: `docs/reality/RAGFlow-D24-2026-09-26.md`.

## Upstream contact state

A review comment was attempted through the current GitHub integration and returned:

```text
403 Resource not accessible by integration
```

So the correct state is **evidence ready, comment not posted**.

## Evidence

- Actions run: https://github.com/hippoley/ContextMesh/actions/runs/36209224821
- Job: https://github.com/hippoley/ContextMesh/actions/runs/36209224821/job/108312116051
- Machine-readable result: `benchmarks/results/ragflow-pr-20207-review-2026-09-26.json`
- Probe: `.github/workflows/reality-ragflow-20207-review.yml`
