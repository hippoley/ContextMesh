# ContextMesh static product surface

This directory is deployed to GitHub Pages by `.github/workflows/pages.yml`.

It is a product/demo surface only. The production runtime is the FastAPI service in `src/contextmesh/`.
